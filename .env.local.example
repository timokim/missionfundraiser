NEXT_PUBLIC_SUPABASE_URL=https://blejbgiifnmvirnmppaw.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJpc3MiOiJzdXBhYmFzZSIsInJlZiI6ImJsZWpiZ2lpZm5tdmlybm1wcGF3Iiwicm9sZSI6ImFub24iLCJpYXQiOjE3MjcyNDIzMTgsImV4cCI6MjA0MjgxODMxOH0.jjOT9l0PEpEV0KA1WZ2ILi2Rce9ZCAZEpddzNlFHh0E
# Used when copying public links from the admin UI (optional locally)
NEXT_PUBLIC_SITE_URL=http://localhost:3000

# Supabase Dashboard → Settings → API → Exposed schemas: add `fundraiser_app`
# (required so PostgREST serves tables and RPCs in that schema).
#
# Migrations apply in order: initial_schema (fundraiser_app only), then storage,
# then auth_bridge (invite-by-email). Storage uses the platform `storage` schema;
# invites read `auth.users` only in the bridge migration.

# Supabase Dashboard → Authentication → URL configuration:
# - Site URL: http://localhost:3000 (and your Vercel URL in production)
# - Redirect URLs: http://localhost:3000/auth/callback (and https://your-domain/auth/callback)
